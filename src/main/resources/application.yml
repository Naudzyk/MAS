management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

server:
  forward-headers-strategy: framework

  mas:
  paths:
    terraform: terraform
    inventory: scripts/inventory.ini
    playbooks: scripts