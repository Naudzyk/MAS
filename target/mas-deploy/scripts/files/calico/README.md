# Bundled Calico install manifest (offline fallback when GitHub is blocked).
# Version must match calico_version in vars.yml.
# Update: curl -fsSL "https://cdn.jsdelivr.net/gh/projectcalico/calico@v3.32.0/manifests/calico.yaml" -o calico-v3.32.0.yaml
