scrape_configs:
  - job_name: 'nodes'
    static_configs:
    - targets: [
        {% for host in groups['all'] %}
        '{{ hostvars[host].ansible_host }}:9100'{% if not loop.last %},{% endif %}
        {% endfor %}
      ]